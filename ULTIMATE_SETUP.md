[{'scripts': {'start': 'node server.js', 'dev': 'nodemon --watch . server.js', 'build': 'node scripts/build.js', 'test': 'npm run security-check && npm run build', 'security-check': 'node scripts/security-check.js', 'backup': 'node scripts/backup.js'}}, {'watch': ['index.html', 'server.js', 'assets/'], 'ext': 'js,html,css', 'delay': 1000}, {'hsts': {'maxAge': 31536000, 'includeSubDomains': True, 'preload': True}, 'frameguard': {'action': 'deny'}, 'noSniff': True, 'xssFilter': True, 'referrerPolicy': {'policy': 'strict-origin-when-cross-origin'}, 'permissionsPolicy': {'Grade': 'A+**\n\n---\n\n## 🐛 Common Issues & Fixes\n\n### Issue: `npm run dev` not working\n**Fix:**\n```bash\nnpm ci\nnpm run dev\n# Should see:', 'http': 'localhost:3000 still works\n\n# 5. Check git status\ngit status\n# Should show: new files (scripts/', 'Issue': 'Vercel build', 'pattern': 'Cause:** `public/_redirects` file present\n**Fix:**\n```bash\nrm public/_redirects 2>/dev/null || true\ngit add -u\ngit commit -m', 'fix': 'remove Netlify _redirects', 'scripts/build.js': 'Cause:** File not committed\n**Fix:**\n```bash\ngit add scripts/build.js\ngit commit -m', 'Vercel': 'it push\n```\n\n---\n\n## 🎯 What Works\n\n### ✅ Development\n- [x] `npm run dev` - Hot reload', 'Status': 'READY TO DEPLOY\n\nAll systems green:\n\n- ✅ `npm run dev` works (tested)\n- ✅ `npm run build` works (tested)\n- ✅ Vercel configuration fixed\n- ✅ Security headers validated\n- ✅ Build script path resolved\n- ✅ Redirects configured correctly\n- ✅ No syntax errors\n- ✅ Dependencies complete\n- ✅ Documentation provided\n\n---\n\n## 📝 Final Checklist Before Push\n\n```bash\n# 1. Run setup (one-time)\nnpm ci\n\n# 2. Test dev server\nnpm run dev\n# → Open http://localhost:3000', 'feat: complete production-ready infrastructure': 8.0, 'push': 1.0, 'order': 1.0}}]